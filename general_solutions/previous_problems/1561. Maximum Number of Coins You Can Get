class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        
        # Time: O(nlogn) | Space: O(1)
        piles.sort()
        n = len(piles)
        
        res = 0
        for i in range(n // 3, n, 2):
            res += piles[i]
        return res
